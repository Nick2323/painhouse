# 📋 ІНСТРУКЦІЯ З ОНОВЛЕННЯ

## ⚠️ ВАЖЛИВО: Потрібне оновлення бази даних!

Код оновлено, але зміни НЕ застосуються автоматично до існуючої бази даних.
Потрібно виконати SQL скрипт оновлення.

---

## 🚀 ШВИДКЕ ОНОВЛЕННЯ (Рекомендовано)

### Метод 1: Автоматичний скрипт
```bash
./update_database.sh
```

### Метод 2: Через phpMyAdmin (найпростіше)
1. Відкрийте **http://localhost:8080** (phpMyAdmin)
2. Увійдіть:
   - **Сервер**: `db`
   - **Користувач**: `root`
   - **Пароль**: `password`
3. Виберіть базу даних **`kogut`** зліва
4. Перейдіть на вкладку **SQL**
5. Скопіюйте весь вміст файлу `apply_updates.sql`
6. Вставте в поле SQL запиту
7. Натисніть **"Виконати"** (Go)

✅ Ви побачите повідомлення про успішне оновлення!

---

## 📝 ЩО ОНОВЛЮЄТЬСЯ?

### 1. 🔐 Пароль адміністратора
- **Старий**: `admin`
- **Новий**: `Admin2025!`
- **Хеш**: `a745fbcf845ec13a3dfd31bf510d64fb1dd57ea7`

### 2. 🖼️ Галерея
- Додається **14 фотографій** з описами
- Фотографії з папки `/gallery/` стануть видимими

### 3. 👥 Описи учасників
- Оновлюються детальні біографії всіх 9 учасників ансамблю

---

## ✅ ПЕРЕВІРКА ПІСЛЯ ОНОВЛЕННЯ

### 1. Адмін-панель
```
URL: http://localhost/site/admin
Логін: admin
Пароль: Admin2025!
```

**Очікуваний результат**:
- ✓ Вхід успішний з новим паролем
- ✓ Перенаправлення на `/admin/dashboard`
- ✓ Відображається чиста адмін-панель без публічного header

### 2. Галерея
```
URL: http://localhost/site/gallery
```

**Очікуваний результат**:
- ✓ Відображається 14 фотографій
- ✓ Кожна фото має опис українською мовою
- ✓ При наведенні з'являється overlay з кнопкою "Переглянути"

### 3. Склад ансамблю
```
URL: http://localhost/site/ensemble
```

**Очікуваний результат**:
- ✓ 9 карток учасників з кольоровими градієнтами
- ✓ Детальні описи при кліку на "Детальніше"
- ✓ Модальне вікно з повною інформацією

### 4. Головна сторінка
```
URL: http://localhost/
```

**Очікуваний результат**:
- ✓ Заголовок "Ансамбль 'Воля'" чітко видимий
- ✓ Немає білої лінії над header
- ✓ Дизайн виглядає професійно

---

## 🐛 ВИРІШЕННЯ ПРОБЛЕМ

### Проблема: "Галерея все ще порожня"
**Причина**: База даних не оновлена
**Рішення**: Виконайте `apply_updates.sql` через phpMyAdmin (метод 2)

### Проблема: "Пароль Admin2025! не працює"
**Причина**: База даних не оновлена
**Рішення**: Використовуйте старий пароль `admin` або оновіть БД

### Проблема: "Після входу все ще показує публічну сторінку"
**Причина**: Кеш браузера або CSS
**Рішення**:
1. Очистіть кеш браузера (Ctrl+Shift+Delete)
2. Виконайте Hard Reload (Ctrl+Shift+R або Cmd+Shift+R)
3. Відкрийте в режимі інкогніто

### Проблема: "Біла лінія все ще є"
**Причина**: Кеш CSS
**Рішення**:
1. Hard Reload: **Ctrl+Shift+R** (Windows) або **Cmd+Shift+R** (Mac)
2. Перевірте в інкогніто режимі
3. Очистіть кеш сайту в DevTools

---

## 📊 ПЕРЕВІРКА СТАТУСУ БД

Виконайте в MySQL/phpMyAdmin:

```sql
-- Перевірка фотографій
SELECT COUNT(*) AS 'Фотографій у галереї' FROM media WHERE Extra = 0;

-- Перевірка паролю
SELECT Login, Password FROM admins WHERE Login = 'admin';

-- Перевірка учасників
SELECT FullName, CHAR_LENGTH(Description) AS 'Довжина опису' FROM members;
```

**Очікувані значення**:
- Фотографій: **14**
- Password: **a745fbcf845ec13a3dfd31bf510d64fb1dd57ea7**
- Довжина опису: **> 200 символів** для кожного учасника

---

## 🔄 АЛЬТЕРНАТИВА: Повне перестворення БД

Якщо оновлення не спрацювало, можна повністю пересоздати БД:

```bash
# УВАГА: Це видалить всі дані!
docker exec -i painhouse_db mysql -uroot -ppassword < /path/to/drop_and_recreate.sql

# Або через phpMyAdmin:
# 1. Drop database `kogut`
# 2. Імпортуйте database.sql повністю
```

---

## 📞 ПІДТРИМКА

Якщо після виконання цих інструкцій проблеми залишаються:

1. Перевірте логи Docker:
   ```bash
   docker logs painhouse_web
   docker logs painhouse_db
   ```

2. Перевірте версію PHP та MySQL:
   ```bash
   docker exec painhouse_web php -v
   docker exec painhouse_db mysql --version
   ```

3. Надішліть скріншоти:
   - Помилки в консолі браузера (F12 → Console)
   - Помилки в Network tab (F12 → Network)
   - Стан бази даних з phpMyAdmin

---

## ✅ УСПІШНО ОНОВЛЕНО?

Після успішного оновлення ви повинні побачити:

- [x] Вхід в адмін-панель працює з `Admin2025!`
- [x] Галерея показує 14 фотографій
- [x] Учасники мають детальні описи
- [x] Заголовок сайту чітко видимий
- [x] Немає білої лінії над header
- [x] Адмін-панель має окремий чистий layout

**Якщо всі пункти виконані - оновлення успішне! 🎉**
