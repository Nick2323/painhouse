# Ансамбль "Воля" - Веб-сайт

Веб-сайт для українського фольклорного ансамблю "Воля" Івано-Франківського обласного громадського товариства «Бойківщина».

## Опис проекту

Сайт призначений для:
- Презентації ансамблю та його історії
- Відображення складу учасників
- Публікації репертуару (близько 30 пісень)
- Галереї фотографій з виступів
- Контактної інформації

## Технічний стек

- **Backend**: PHP 5.6+ з Yii Framework 1.x
- **База даних**: MySQL 5.6+
- **Frontend**: Bootstrap 3, jQuery
- **Структура**: MVC архітектура

## Вимоги

### Варіант 1: Ручна установка
- PHP 5.6 або вище (рекомендовано 7.4)
- MySQL 5.6 або вище / MariaDB
- Apache/Nginx веб-сервер з mod_rewrite
- Розширення PHP: mysqli, pdo, pdo_mysql, mbstring

### Варіант 2: Docker (рекомендовано)
- Docker
- Docker Compose

## Установка

### Варіант 1: Docker (найлегший спосіб)

1. Клонуйте репозиторій:
```bash
git clone <repository-url>
cd painhouse
```

2. Запустіть Docker контейнери:
```bash
docker-compose up -d
```

3. Імпортуйте базу даних:
```bash
docker-compose exec db mysql -u root -proot < database.sql
```

4. Відкрийте браузер і перейдіть до:
```
http://localhost:8080
```

### Варіант 2: Ручна установка (XAMPP/LAMP/WAMP)

1. Клонуйте репозиторій у директорію веб-сервера:
```bash
cd /var/www/html  # або C:\xampp\htdocs для Windows
git clone <repository-url> painhouse
cd painhouse
```

2. Створіть базу даних:
```bash
mysql -u root -p < database.sql
```

Або імпортуйте через phpMyAdmin:
- Відкрийте phpMyAdmin
- Створіть нову базу даних `kogut`
- Імпортуйте файл `database.sql`

3. Налаштуйте конфігурацію бази даних у файлі `protected/config/main.php`:
```php
'db'=>array(
    'connectionString' => 'mysql:host=localhost;dbname=kogut',
    'emulatePrepare' => true,
    'username' => 'root',
    'password' => 'ваш_пароль',
    'charset' => 'utf8',
),
```

4. Встановіть права доступу:
```bash
chmod -R 755 /var/www/html/painhouse
chmod -R 777 /var/www/html/painhouse/photo
chmod -R 777 /var/www/html/painhouse/gallery
chmod -R 777 /home/user/painhouse/protected/runtime
```

5. Перевірте, що mod_rewrite увімкнено в Apache:
```bash
sudo a2enmod rewrite
sudo service apache2 restart
```

6. Відкрийте браузер і перейдіть до:
```
http://localhost/painhouse
```

## Дані для входу в адмін-панель

- **URL**: http://localhost:8080/site/admin (або http://localhost/painhouse/site/admin)
- **Логін**: `admin`
- **Пароль**: `admin123`

**⚠️ ВАЖЛИВО**: Після першого входу обов'язково змініть пароль через адмін-панель!

## Структура проекту

```
painhouse/
├── css/                    # CSS стилі (Bootstrap, custom)
├── framework/              # Yii Framework 1.x
├── gallery/                # Фотографії для галереї
├── images/                 # Зображення для сайту
├── java/                   # JavaScript файли
├── js/                     # Додаткові JS файли
├── jc/                     # jQuery та Bootstrap JS
├── photo/                  # Фотографії учасників ансамблю
├── protected/
│   ├── config/             # Конфігурація
│   │   ├── main.php        # Головний конфіг
│   │   └── console.php     # Конфіг для консолі
│   ├── controllers/        # Контролери
│   │   ├── SiteController.php
│   │   └── AdminController.php
│   ├── models/             # Моделі даних
│   ├── views/              # Представлення
│   │   ├── admin/          # Адмін-панель
│   │   ├── layouts/        # Шаблони
│   │   └── site/           # Публічні сторінки
│   └── runtime/            # Тимчасові файли
├── .htaccess               # Правила URL rewriting
├── index.php               # Точка входу
├── database.sql            # SQL схема та початкові дані
└── README.md               # Цей файл
```

## Функціонал адмін-панелі

### Управління учасниками ансамблю
- Додавання нових учасників з фото та описом
- Редагування інформації про учасників
- Видалення учасників

### Управління репертуаром
- Додавання пісень з категоризацією
- Створення нових категорій
- Видалення пісень

### Управління медіа
- Завантаження фотографій (JPG, PNG, JPEG)
- Завантаження відео (MP4)
- Завантаження аудіо (MP3)
- Видалення медіафайлів

### Безпека
- Зміна пароля адміністратора
- SHA1 хешування паролів
- Авторизація для всіх адмін-дій

## Структура бази даних

### Таблиці:
- **admins** - адміністратори системи
- **members** - учасники ансамблю
- **repertoire** - каталог пісень з категоріями
- **media** - медіафайли (фото/відео/аудіо)

## Розробка

### Вимкнення debug режиму
У файлі `index.php` закоментуйте:
```php
// defined('YII_DEBUG') or define('YII_DEBUG',true);
// defined('YII_TRACE_LEVEL') or define('YII_TRACE_LEVEL',3);
```

### Логування
Логи зберігаються в `protected/runtime/application.log`

## Вирішення проблем

### Помилка 404 на всіх сторінках
- Перевірте, що mod_rewrite увімкнено
- Перевірте налаштування .htaccess
- Перевірте конфігурацію urlManager у main.php

### Помилка підключення до бази даних
- Перевірте правильність даних у protected/config/main.php
- Переконайтеся, що MySQL запущений
- Перевірте, що база даних `kogut` створена

### Помилки завантаження файлів
- Перевірте права доступу до директорій photo/ та gallery/
- Збільште upload_max_filesize та post_max_size у php.ini

### Не відображаються стилі
- Перевірте шляхи до CSS файлів у layout
- Очистіть кеш браузера

## Зупинка Docker контейнерів

```bash
docker-compose down
```

Для повного видалення з даними:
```bash
docker-compose down -v
```

## Підтримка

Для питань та підтримки звертайтесь:
- Email: larysonka@yandex.ua
- Телефон: +38(050)689-48-50

## Ліцензія

Проект створено для Ансамблю "Воля" © 2004-2025

---

**Створено з любов'ю до української культури** 🇺🇦
